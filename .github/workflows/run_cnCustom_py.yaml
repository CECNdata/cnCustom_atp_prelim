name: 'run_cnCustom_py'

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */4 18-25 * *'


env:
  repo_name: CECNdata/cnCustom_atp_prelim
  repo_token: ${{ secrets.repo_token }}
  dl_path: ./.github/*py
  docker_image: ghcr.io/cecndata/ga-sec:latest
  retry: 2
  

jobs:
  download:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout codes
        uses: actions/checkout@v3
        with:
          repository: '${{ env.repo_name }}'
           # `GH_PAT` is a secret that contains you PAT
          ref: main

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GH_PAT }}
      - name: Get Docker image digest
        id: docker-digest
        run: |
          DIGEST=$(docker manifest inspect ${{ env.docker_image }} --verbose | jq -r '.Descriptor.digest')
          echo "digest=$DIGEST" >> $GITHUB_OUTPUT
      - uses: actions/cache@v3
        id: docker-cache
        with:
          path: /tmp/ga-sec_latest.tar
          key: ga-sec-latest-${{ steps.docker-digest.outputs.digest }}
          restore-keys: |
            ga-sec-latest-
      - if: steps.docker-cache.outputs.cache-hit != 'true'
        run: |
            docker pull ${{ env.docker_image }}
            docker save ${{ env.docker_image }} -o /tmp/ga-sec_latest.tar
      - if: steps.docker-cache.outputs.cache-hit == 'true'
        run: docker load -i /tmp/ga-sec_latest.tar
      - name: Export all GitHub env to Docker env file
        run: |
          env > /tmp/github_env.list
      - name: Run customs download in Docker
        continue-on-error: true
        run: |
          docker run --rm \
            --env-file /tmp/github_env.list \
            -v ${{ github.workspace }}:/workspace \
            -w /workspace \
            ${{ env.docker_image }} \
            timeout 1200 xvfb-run python ${{ env.dl_path}}
            

# jobs:
#   release_day:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout codes
#         uses: actions/checkout@v3
#         with:
#           repository: '${{ env.repo_name }}'
#            # `GH_PAT` is a secret that contains you PAT
#           ref: main

#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: 3.11

#       - name: Install dependencies
#         run: |
#           sudo apt-get update && sudo apt-get install libdbus-glib-1-2 -y
#           cd .github
#           python -m pip install --upgrade pip
#           # 禁用root警告（CI环境下可接受）
#           pip install fire pipreqs --root-user-action=ignore
#           # 生成requirements.txt并清理冲突版本
#           pipreqs . --force --encoding=utf-8
#           # 移除冲突的playwright/numpy版本，写入兼容版本
#           sed -i '/playwright/d' requirements.txt
#           sed -i '/numpy/d' requirements.txt
#           echo "playwright==1.40.0" >> requirements.txt
#           echo "numpy==2.4.3" >> requirements.txt  # Python3.11兼容，若用3.10则改为2.2.6
#           # 安装依赖
#           pip install -r requirements.txt --root-user-action=ignore

#       - name: Install Playwright
#         run: |
#           # 安装Firefox系统依赖
#           npx playwright install-deps firefox
#           # Playwright 1.40.0不支持@版本号，直接安装默认绑定的121.0版本
#           python -m playwright install firefox

#       - name: check ip
#         run: |
#             curl ifconfig.me
            
#       - name: run customs download
#         continue-on-error: true
#         run: |
#            cp ./.github/*py ./
#            DEBUG=pw:api timeout 1800 xvfb-run python *py
